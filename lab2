CREATE DATABASE lab_2
\c lab_2

CREATE TABLE  employees(
    employee_id SERIAL PRIMARY KEY,
    first_name VARCHAR(30),
    last_name VARCHAR(40),
    department_id INTEGER,
    salary INTEGER
);

INSERT INTO employees (first_name, last_name, department_id, salary)
VALUES ('Diana','Galimzhankyzy', 7,7777777);

INSERT INTO employees ( employee_id, first_name, last_name)
VALUES (DEFAULT, 'Bob', 'Smit');

INSERT INTO employees (first_name, last_name, department_id, salary)
VALUES ('Sabina', 'Nurmagambetova', NULL, 11111111);

INSERT INTO employees (first_name, last_name, department_id, salary)
VALUES ('Alexander', 'Santechnik', 5, 300),
       ('Varvara', 'Lillison', 3, 999999),
       ('Justin', 'Bieber', 9, 100000),
       ('Vlad','Eshkere',6, 100),
       ('Adriana', 'Lima', 8, 88888);

ALTER TABLE employees ALTER COLUMN  first name SET DEFAULT 'John';

INSERT INTO employees (first_name, last_name, department_id, salary)
VALUES ('Ivy', NULL, 60000);

INSERT INTO employees DEFAULT VALUES;

CREATE TABLE employees_archive(LIKE employees INCLUDING ALL);

INSERT INTO employees_achive SELECT * FROM employees;

UPDATE employees
SET department_id = 1
WHERE department_id IS NULL;

SELECT first_name, last_name, salary * 1.15 AS "Updates Salary"
FROM employees;

DELETE FROM employees
WHERE salary < 50000;

DELETE FROM employees_archive
WHERE employee_id IN (SELECT employee_id FROM employees)
RETURNING *;


DELETE FROM employees
RETURNING *;
